---
import type { Service } from '@lib/content';

interface Props extends Service {}

const { title, description, duration } = Astro.props;
---
<article class="glass-panel flex h-full flex-col gap-4 rounded-2xl p-6">
  <div class="flex items-center justify-between">
    <h3 class="text-xl font-semibold text-cocoa dark:text-cream">{title}</h3>
    <span class="rounded-pill bg-caramel/20 px-3 py-1 text-xs font-semibold text-caramel dark:bg-cocoa/60">{duration}</span>
  </div>
  <p class="text-sm text-cocoa/80 dark:text-cream/80">{description}</p>
  <div class="mt-auto flex items-center gap-2 text-sm font-semibold text-tawny">
    Agenda disponible
    <span aria-hidden="true">→</span>
  </div>
</article>
